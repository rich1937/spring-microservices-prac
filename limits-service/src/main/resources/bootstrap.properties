spring.application.name=limits-service
spring.cloud.config.uri=http://localhost:8888
spring.profiles.active=qa
#no need to configure since 8080 is a default port
#server.port=8080
#application.properties file has been renamed to bootstrap.properteis file when below 
#properties were switched to pick up from springcloud server.
#below limits-service properties should be picked up 
#from spring cloud, so no need to configure here.
#limits-service.minimum=99
#limits-service.maximum=9999

##copied from spring boot in 10 steps
logging.level.org.springframework=TRACE;
#management.endpoints.web.exposure.include=*
#management.trace.http.enabled: true
eureka.client.service-url.default-zone=http://localhost:8761/eureka
